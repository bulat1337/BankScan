interface:
  display_name: "Bank Balance Bridge"
  short_description: "Use bankscan to wait for login, scan balances, and close bridge Chrome"
  default_prompt: "Use $bank-balance-bridge with bankscan or the bundled CLI to open the persistent bridge profiles for Alfa and VTB, wait for login when needed, scan balances, and close bridge Chrome at the end."

policy:
  allow_implicit_invocation: true
